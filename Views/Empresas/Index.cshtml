@model IEnumerable<Organizacional.Models.Empresa>

@{
    ViewData["Title"] = "Empresas";
}
@{
    var idUsuario = Context.Session.GetInt32("IdUsuario");
    var rolUsuario = Context.Session.GetInt32("Rol");
}

@if (idUsuario == null || rolUsuario == null)
{
    <script>window.location.href = '/Auth/Login';</script>
}

@await Html.PartialAsync("_Sidebar")

<!DOCTYPE html>
<html lang="es">
<head>
    <m@model IEnumerable<Organizacional.Models.Empresa>

@{
    ViewData["Title"] = "Empresas";
}

@{
    var idUsuario = Context.Session.GetInt32("IdUsuario");
    var rolUsuario = Context.Session.GetInt32("Rol");
}

@if (idUsuario == null || rolUsuario == null)
{
    <script>window.location.href = '/Auth/Login';</script>
}

@await Html.PartialAsync("_Sidebar")

<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8" />
    <title>@ViewData["Title"]</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet" />
    <link rel="stylesheet" href="~/css/Historial.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/css/Dashboard.css" asp-append-version="true" />
</head>
<body>
    <!-- Contenido principal -->
    <div class="col-md-9 col-lg-10 p-4 contenido" style="margin-left: 240px;">
        <h2 class="mb-4">Empresas</h2>

        <a asp-action="Crear" class="btn btn-success mb-3">
            <i class="bi bi-plus-circle"></i> Agregar Empresa
        </a>

        <!-- 📋 Vista en tabla (solo escritorio) -->
        <div class="table-responsive d-none d-md-block">
            <table class="table table-bordered">
                <thead class="table-light">
                    <tr>
                        <th>ID</th>
                        <th>Nombre</th>
                        <th style="width: 200px;">Acciones</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var empresa in Model)
                    {
                        <tr>
                            <td>@empresa.IdEmpresa</td>
                            <td>@empresa.Nombre</td>
                            <td>
                                <a asp-action="Editar" asp-route-id="@empresa.IdEmpresa" class="btn btn-sm btn-warning">
                                    <i class="bi bi-pencil"></i> Editar
                                </a>
                                <a asp-action="Eliminar" asp-route-id="@empresa.IdEmpresa" class="btn btn-sm btn-danger ms-2">
                                    <i class="bi bi-trash"></i> Eliminar
                                </a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>

        <!-- 📱 Vista móvil tipo cards -->
        <div class="d-md-none">
            @foreach (var empresa in Model)
            {
                <div class="card mb-3 shadow-sm">
                    <div class="card-body">
                        <h6 class="card-title">
                            <i class="bi bi-building text-primary"></i> <strong>@empresa.Nombre</strong>
                        </h6>
                        <p class="mb-2"><strong>ID:</strong> @empresa.IdEmpresa</p>
                        <div>
                            <a asp-action="Editar" asp-route-id="@empresa.IdEmpresa" class="btn btn-sm btn-warning me-2">
                                <i class="bi bi-pencil"></i> Editar
                            </a>
                            <a asp-action="Eliminar" asp-route-id="@empresa.IdEmpresa" class="btn btn-sm btn-danger">
                                <i class="bi bi-trash"></i> Eliminar
                            </a>
                        </div>
                    </div>
                </div>
            }
        </div>
    </div>
</body>
</html>
eta charset="utf-8" />
    <title>@ViewData["Title"]</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet" />
    <link rel="stylesheet" href="~/css/Historial.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/css/Dashboard.css" asp-append-version="true" />
</head>
<body>
        <!-- Contenido principal -->

    <div class="col-md-9 col-lg-10 p-4 contenido" style="margin-left: 240px;">
        <h2 class="mb-4">Empresas</h2>

        <a asp-action="Crear" class="btn btn-success mb-3">
            <i class="bi bi-plus-circle"></i> Agregar Empresa
        </a>

        <table class="table table-bordered">
            <thead class="table-light">
                <tr>
                    <th>ID</th>
                    <th>Nombre</th>
                    <th style="width: 200px;">Acciones</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var empresa in Model)
                {
                    <tr>
                        <td>@empresa.IdEmpresa</td>
                        <td>@empresa.Nombre</td>
                        <td>
                            <a asp-action="Editar" asp-route-id="@empresa.IdEmpresa" 
                            class="btn btn-sm btn-warning">
                                <i class="bi bi-pencil"></i> Editar
                            </a>
                            <a asp-action="Eliminar" asp-route-id="@empresa.IdEmpresa" 
                            class="btn btn-sm btn-danger ms-2">
                                <i class="bi bi-trash"></i> Eliminar
                            </a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</body>
</html>