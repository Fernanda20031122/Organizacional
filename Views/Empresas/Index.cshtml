@model IEnumerable<Organizacional.Models.Empresa>

@{
    ViewData["Title"] = "Empresas";
}

<div class="col-md-9 col-lg-10 p-4 contenido" style="margin-left: 240px;">
    <h2 class="mb-4">Empresas</h2>

    <a asp-action="Crear" class="btn btn-success mb-3">
        <i class="bi bi-plus-circle"></i> Agregar Empresa
    </a>

    <table class="table table-bordered table-striped">
        <thead class="table-dark">
            <tr>
                <th>ID</th>
                <th>Nombre</th>
                <th style="width: 200px;">Acciones</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var empresa in Model)
            {
                <tr>
                    <td>@empresa.IdEmpresa</td>
                    <td>@empresa.Nombre</td>
                    <td>
                        <a asp-action="Editar" asp-route-id="@empresa.IdEmpresa" 
                           class="btn btn-sm btn-warning">
                            <i class="bi bi-pencil"></i> Editar
                        </a>
                        <a asp-action="Eliminar" asp-route-id="@empresa.IdEmpresa" 
                           class="btn btn-sm btn-danger ms-2">
                            <i class="bi bi-trash"></i> Eliminar
                        </a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>